<!-- Thanks for contributing to MemTab. Keep this short — a couple of sentences is fine. -->

## What this changes

## Why

## How I tested it

<!--
`npm run check` covers the pure logic. Anything touching the content script, the
service worker, or the favicon renderer also needs a manual pass — please say which
sites you loaded it on. A plain page, an SPA that rewrites its own favicon, and a page
with no favicon at all is a good spread.
-->

- [ ] `npm run check` passes
- [ ] Loaded unpacked and verified manually (say where)
- [ ] Updated docs if behavior or settings changed
